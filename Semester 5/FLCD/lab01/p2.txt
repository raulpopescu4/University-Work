def findMin(int a, int b, int c)
{
    int min_val;
	min_val := a;
    
    if (b < min _val){
        min_val := b;
		}

    if (c < min_val){
        min_val := c;
    }

    print("Maximum value is: ", max_val);
    print("Minimum value is: ", min_val);
}